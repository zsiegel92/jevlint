# TypeScript smoke marker

TypeScript source must not contain `JEVLINT_SMOKE_BAD`.

---

Fail exactly when the file contains `JEVLINT_SMOKE_BAD`. For location, select only lines containing that exact text.
